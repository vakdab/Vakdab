// ===== COMMUNITY CHAT =====
// Оригінальні рядки: L8346-L9108
//
// Містить:
//   function _renderReplyBanner() / _setReplyTo(m)
//   function _uniqueCommunityAuthors() / _highlightMentions(escapedText)
//   function getMyEarnedAchievements()
//   function initCommunity()
//   function _setupCompose(user) / updateInputVisibility() / refreshExtra()
//   function doSend() / _renderMediaPreview(media, container)
//   async function _sendMessage(user, extra, onSent)
//   function _renderComMessages(currentUser)
//   async function _toggleReaction(msgId, emoji, currentUser)
//   function _closeMsgContextMenu() / _showMsgContextMenu(m, currentUser, x, y)
//   function _subscribeToChat(currentUser) — real-time Firestore підписка

