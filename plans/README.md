# Animation Improvement Plans — VakDab

Generated by the `improve-animations` skill audit (emilkowalski/skills). Commit stamped: `e665b8d`.

## Audit summary

Vetted findings, ordered by leverage (impact ÷ effort). All verified at their `file:line`.

| # | Severity | Category | Location | Finding |
| --- | --- | --- | --- | --- |
| 1 | HIGH | Easing & duration | `hero.css:461,628`, `anime-card.css:31` | Homepage grid entrances `fadeInUp 0.5s ease` — 500ms weak curve, above the 300ms UI budget, on the most-visited page |
| 2 | MEDIUM | Easing & duration | `anime-card.css:67` | Poster hover zoom `transform 0.4s ease` — floaty, above the ≤200ms hover budget |
| 3 | MEDIUM | Performance | `hero.css:406-411` | Carousel dots animate `width` (layout) via `transition: all 0.3s ease`; should be `transform: scaleX` |
| 4 | MEDIUM | Performance | `chat.css` (13 rules) | `transition: all` on high-frequency chat controls — animates off-GPU props |
| 5 | MEDIUM | Performance & cohesion | `search.css:28`, `back-to-top.css:21`, `filter.css:29`, `hero.css:277` | Remaining `transition: all` + non-repo Material/bare-`ease` curves |

Lower priority (not planned yet): `video-overlay.css:263` scrubber thumb hover reveal from `scale(0)` (LOW polish — target `scale(0.5)` + opacity); scattered hand-typed curves duplicating `--ui-ease`/`--transition` tokens (LOW cohesion).

Non-findings worth noting: `prefers-reduced-motion` is already handled site-wide by the catch-all in `ux-2026.css:402` (though it nulls all motion rather than keeping gentle fades — acceptable); `:active` press feedback (`scale(.95-.97)`) is correct everywhere; the 5 `ease-in` hits are all `ease-in-out` on ambient infinite loops (correct); hero background 700ms and slide 900ms are marketing-appropriate (not reported).

## Missed opportunities (additive, not yet planned)

1. **Homepage grid stagger** — cards enter all-at-once; a 30–80ms `animation-delay` per card would feel deliberate, but needs per-card ordering (markup/JS), so it's excluded from plan 001.
2. **Schedule/date section reveals** — layout jumps when day sections toggle; a brief opacity/transform transition would prevent jarring changes.

## Plans

| # | Title | Severity | Status |
| --- | --- | --- | --- |
| 001 | Shorten homepage grid entrance to 300ms strong ease-out | HIGH | DONE |
| 002 | Speed up card poster hover zoom to 180ms ease-out | MEDIUM | DONE |
| 003 | Animate hero carousel dots with transform scaleX instead of width | MEDIUM | DONE |
| 004 | Replace transition:all with explicit GPU-safe properties in chat | MEDIUM | DONE |
| 005 | Replace remaining transition:all and unify curves across pages/components | MEDIUM | DONE |

### Extended beyond the five plans (same finding class)

While mechanically re-checking the audit criteria after execution, the same `fadeInUp 0.5s ease` entrance and `transform 0.4s ease` hover patterns surfaced in files outside the original five plans' scope. They were fixed in the same pass:

- `main.css:30` — `.popular-card` entrance `fadeInUp 0.5s ease` → `.3s var(--ui-ease) both` (duplicate rule, same as plan 001's `anime-card.css:31`)
- `main.css:66` — `.popular-card:hover img` zoom `transform 0.4s ease` → `180ms var(--ui-ease)` (duplicate rule, same as plan 002's `anime-card.css:67`)
- `settings.css:692`, `profile.css:1162`, `profile.css:1172`, `schedule.css:389`, `schedule.css:591` — SPA page-container entrances (`#profilePageContainer`, `#genrePageContainer`, `#genresPageContainer`, `#searchPageContainer`, `#settingsPageContainer`) `fadeInUp 0.5s ease` → `.3s var(--ui-ease)`; these play on every page switch, so they were well above budget

## Recommended execution order

1. **001** first — highest leverage, touches the most-visited page, 3-line change.
2. **002** — independent, 1-line change.
3. **003** — independent, 1 rule.
4. **004** — independent, one file.
5. **005** — independent, four files.

All five are independent (no shared files, no ordering dependency). Run them in any order; 001 first for the biggest feel win. After execution, re-run the audit (`reconcile`) to mark plans DONE and refresh stale line references.
